<?xml version = "1.0" encoding = "UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace = "Bank">

    <select id = "checkPayment" parameterType = "map" resultType = "String">
        SELECT VirtualAccountOfPayment FROM PURCHASE WHERE VirtualAccountOfPayment = #{bank1}
        OR VirtualAccountOfPayment = #{bank2}
        OR VirtualAccountOfPayment = #{bank3}
        OR VirtualAccountOfPayment = #{bank4} LIMIT 1;
    </select>

    <insert id = "insert" parameterType = "map">
        INSERT INTO PURCHASE (PurchaseId, Username, TotalPrice, VirtualAccountOfPayment) VALUES (#{1}, #{2}, #{3}, #{4});
    </insert>

    <resultMap id = "result" type = "main.java.backend.model.PurchaseFlight">
        <result property = "id" column = "ID"/>
        <result property = "username" column = "Username"/>
        <result property = "password" column = "Password"/>
        <result property = "name" column = "Name"/>
        <result property = "points" column = "Points"/>
    </resultMap>

</mapper>